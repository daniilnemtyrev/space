import React from 'react';
import styled from 'styled-components';

export const Factions = () => {
    return (
        <Wrapper>
        </Wrapper>

    );
};

const Wrapper = styled.div`
height: 100vh;
background: url('backgrounds/factions.jpg');
background-size: cover;
`;
